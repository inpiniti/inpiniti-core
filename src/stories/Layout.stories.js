import { Layout } from "./Layout";
import { Header } from "./Header";

import { LoggedIn } from "./Header.stories";

export default {
  title: "Layout",
  component: Layout,
};

export const Empty = {
  args: {},
};
